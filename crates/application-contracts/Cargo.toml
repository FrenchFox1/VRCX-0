[package]
name = "vrcx-0-application-contracts"
version = "0.1.0"
edition = "2021"
license.workspace = true
description = "Narrow application boundary contracts"

[dependencies]
serde = { workspace = true, features = ["derive"] }
specta = { workspace = true }
vrcx-0-core = { path = "../core" }

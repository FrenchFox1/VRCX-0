[package]
name = "vrcx-0-runtime-event"
version = "0.1.0"
edition = "2021"
license.workspace = true
description = "Runtime event payload contract and instance roster push port"

[dependencies]
serde = { workspace = true, features = ["derive"] }
specta = { workspace = true }
vrcx-0-core = { path = "../core" }
